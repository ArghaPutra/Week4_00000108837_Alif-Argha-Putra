import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    paddingTop: 10,
  },
  card: {
    width: 325,
    marginVertical: 6,
    borderRadius: 10,
    elevation: 3,
  },
});

export default styles;